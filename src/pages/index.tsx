import UrlForm from '../components/UrlForm';

const HomePage = () => {
  return (
    <div style={{ padding: '20px' }}>
      <h1>URL Shortener</h1>
      <UrlForm />
    </div>
  );
};

export default HomePage;
